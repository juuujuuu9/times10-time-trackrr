---
import Layout from '../layouts/Layout.astro';
import { requireAuth } from '../utils/session';
import Timer from '../components/Timer';

// Require authentication
const user = await requireAuth('/login')(Astro) as any;
---

<Layout title="Timer Persistence Test - Times10 Time Tracker">
  <div class="min-h-screen bg-gray-50 py-8">
    <div class="max-w-4xl mx-auto px-4 sm:px-6 lg:px-8">
      <div class="text-center mb-8">
        <h1 class="text-3xl font-bold text-gray-900 mb-4">Timer Persistence Test</h1>
        <p class="text-lg text-gray-600">
          Test the timer persistence functionality - start a timer, then refresh the page or close and reopen the browser.
        </p>
      </div>

      <div class="bg-white rounded-xl shadow-sm border border-gray-200 p-8">
        <h2 class="text-xl font-semibold text-gray-900 mb-6">Persistent Timer</h2>
        <Timer client:load />
      </div>

      <div class="mt-8 bg-white rounded-xl shadow-sm border border-gray-200 p-6">
        <h3 class="text-lg font-semibold text-gray-900 mb-4">Persistence Features:</h3>
        <div class="space-y-3 text-sm text-gray-600">
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Automatic Save:</strong> Timer state is automatically saved to localStorage</p>
          </div>
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Page Refresh:</strong> Timer continues running after page refresh</p>
          </div>
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Browser Close:</strong> Timer state persists when browser is closed and reopened</p>
          </div>
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Task Selection:</strong> Selected task is remembered across sessions</p>
          </div>
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Accurate Time:</strong> Timer shows correct elapsed time even after page reload</p>
          </div>
          <div class="flex items-start">
            <span class="w-6 h-6 bg-green-100 text-green-600 rounded-full flex items-center justify-center text-xs font-medium mr-3 mt-0.5">✓</span>
            <p><strong>Auto Cleanup:</strong> Expired timers (24+ hours) are automatically cleared</p>
          </div>
        </div>
      </div>

      <div class="mt-8 bg-yellow-50 border border-yellow-200 rounded-lg p-4">
        <h4 class="text-sm font-medium text-yellow-800 mb-2">Test Instructions:</h4>
        <ol class="text-sm text-yellow-700 space-y-1 list-decimal list-inside">
          <li>Select a task and start the timer</li>
          <li>Wait a few seconds to see the timer counting</li>
          <li>Refresh the page (F5 or Ctrl+R)</li>
          <li>Notice the timer continues from where it left off</li>
          <li>Try closing the browser tab and reopening it</li>
          <li>The timer should still be running with the correct time</li>
        </ol>
      </div>

      <div class="mt-8 text-center space-x-4">
        <a 
          href="/dashboard" 
          class="inline-flex items-center px-4 py-2 border border-transparent text-sm font-medium rounded-md text-white bg-blue-600 hover:bg-blue-700 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
        >
          Back to Dashboard
        </a>
        <a 
          href="/test-enhanced-timer" 
          class="inline-flex items-center px-4 py-2 border border-gray-300 text-sm font-medium rounded-md text-gray-700 bg-white hover:bg-gray-50 focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-blue-500"
        >
          Enhanced Timer Test
        </a>
      </div>
    </div>
  </div>
</Layout>
