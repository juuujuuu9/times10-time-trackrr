---
import AdminLayout from '../../../layouts/AdminLayout.astro';
import { requireRole } from '../../../utils/session';

// Get current user
const user = await requireRole('admin', '/dashboard')(Astro) as any;
---

<AdminLayout title="Settings" currentPage="settings">
  <div class="space-y-8">
    <!-- Header -->
    <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 p-6">
      <div class="flex items-center justify-between">
        <div>
          <h1 class="text-3xl font-bold text-white">System Settings</h1>
          <p class="text-gray-300 mt-2">Configure your organization and system preferences</p>
        </div>
        <div class="flex items-center space-x-3">
          <div class="w-3 h-3 bg-gray-600 rounded-full animate-pulse"></div>
          <span class="text-sm text-gray-400">System Online</span>
        </div>
      </div>
    </div>

    <!-- Settings Sections -->
    <div class="grid grid-cols-1 lg:grid-cols-2 gap-8">
      <!-- Organization Information -->
      <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
        <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
          <div class="flex items-center">
            <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
              <span class="text-white text-xl">🏢</span>
            </div>
            <h3 class="text-xl font-semibold text-white">Organization Information</h3>
          </div>
        </div>
        <div class="p-6">
          <form id="orgForm" class="space-y-6">
            <div>
              <label for="orgName" class="block text-sm font-semibold text-gray-300 mb-2">
                Organization Name
              </label>
              <input
                type="text"
                id="orgName"
                value="Times10 Time Tracker"
                class="w-[calc(100%-2rem)] px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
                placeholder="Enter organization name"
              />
            </div>
            <div>
              <label for="orgEmail" class="block text-sm font-semibold text-gray-300 mb-2">
                Contact Email
              </label>
              <input
                type="email"
                id="orgEmail"
                value="admin@times10.com"
                class="w-[calc(100%-2rem)] px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
                placeholder="Enter contact email"
              />
            </div>
            <div>
              <label for="orgWebsite" class="block text-sm font-semibold text-gray-300 mb-2">
                Website
              </label>
              <input
                type="url"
                id="orgWebsite"
                value="https://times10.com"
                class="w-[calc(100%-2rem)] px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
                placeholder="Enter website URL"
              />
            </div>
            <div>
              <label for="orgLogo" class="block text-sm font-semibold text-gray-300 mb-2">
                Logo URL
              </label>
              <input
                type="url"
                id="orgLogo"
                value="https://times10.com/logo.png"
                class="w-[calc(100%-2rem)] px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
                placeholder="Enter logo URL"
              />
            </div>
            <div class="flex justify-end pt-4">
              <button
                type="submit"
                class="bg-gradient-to-r from-gray-300 to-gray-400 hover:from-gray-400 hover:to-gray-500 text-gray-900 font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-105 shadow-lg"
              >
                💾 Save Organization Info
              </button>
            </div>
          </form>
              </div>
    </div>

    <!-- User Profile -->
    <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
      <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
        <div class="flex items-center">
          <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
            <span class="text-white text-xl">👤</span>
          </div>
          <h3 class="text-xl font-semibold text-white">Your Profile</h3>
        </div>
      </div>
      <div class="p-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Full Name
              </label>
              <div class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white">
                {user?.name || 'Unknown'}
              </div>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Email Address
              </label>
              <div class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white">
                {user?.email || 'No email'}
              </div>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Role
              </label>
              <div class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-semibold bg-gray-600 text-white">
                  {user?.role ? user.role.charAt(0).toUpperCase() + user.role.slice(1) : 'Unknown'}
                </span>
              </div>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Status
              </label>
              <div class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg">
                <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-semibold bg-gray-600 text-white">
                  {user?.status ? user.status.charAt(0).toUpperCase() + user.status.slice(1) : 'Unknown'}
                </span>
              </div>
            </div>
          </div>
          <div class="space-y-4">
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Account Actions
              </label>
              <div class="space-y-3">
                <button class="w-full px-4 py-3 bg-gray-300 hover:bg-gray-400 text-gray-900 font-medium rounded-lg transition-colors">
                  🔐 Change Password
                </button>
                <button class="w-full px-4 py-3 bg-gray-300 hover:bg-gray-400 text-gray-900 font-medium rounded-lg transition-colors">
                  📧 Update Email
                </button>
                <button class="w-full px-4 py-3 bg-gray-300 hover:bg-gray-400 text-gray-900 font-medium rounded-lg transition-colors">
                  👤 Edit Profile
                </button>
              </div>
            </div>
            <div>
              <label class="block text-sm font-semibold text-gray-300 mb-2">
                Session Information
              </label>
              <div class="px-4 py-3 bg-gray-700 border border-gray-600 rounded-lg text-white text-sm">
                <div class="flex justify-between items-center">
                  <span>Last Login:</span>
                  <span class="text-gray-300">Just now</span>
                </div>
                <div class="flex justify-between items-center mt-2">
                  <span>Session ID:</span>
                  <span class="text-gray-300 font-mono text-xs">••••••••</span>
                </div>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>

    <!-- System Preferences -->
      <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
        <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
          <div class="flex items-center">
            <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
              <span class="text-white text-xl">⚙️</span>
            </div>
            <h3 class="text-xl font-semibold text-white">System Preferences</h3>
          </div>
        </div>
        <div class="p-6">
          <form id="systemForm" class="space-y-6">
            <div>
              <label for="timezone" class="block text-sm font-semibold text-gray-300 mb-2">
                Default Timezone
              </label>
              <select
                id="timezone"
                class="w-full px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
              >
                <option value="UTC">UTC</option>
                <option value="America/New_York" selected>Eastern Time (ET)</option>
                <option value="America/Chicago">Central Time (CT)</option>
                <option value="America/Denver">Mountain Time (MT)</option>
                <option value="America/Los_Angeles">Pacific Time (PT)</option>
                <option value="Europe/London">London (GMT)</option>
                <option value="Europe/Paris">Paris (CET)</option>
                <option value="Asia/Tokyo">Tokyo (JST)</option>
              </select>
            </div>
            <div>
              <label for="dateFormat" class="block text-sm font-semibold text-gray-300 mb-2">
                Date Format
              </label>
              <select
                id="dateFormat"
                class="w-full px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
              >
                <option value="MM/DD/YYYY" selected>MM/DD/YYYY</option>
                <option value="DD/MM/YYYY">DD/MM/YYYY</option>
                <option value="YYYY-MM-DD">YYYY-MM-DD</option>
              </select>
            </div>
            <div>
              <label for="timeFormat" class="block text-sm font-semibold text-gray-300 mb-2">
                Time Format
              </label>
              <select
                id="timeFormat"
                class="w-full px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
              >
                <option value="12" selected>12-hour (AM/PM)</option>
                <option value="24">24-hour</option>
              </select>
            </div>
            <div>
              <label for="weekStart" class="block text-sm font-semibold text-gray-300 mb-2">
                Week Start Day
              </label>
              <select
                id="weekStart"
                class="w-full px-4 py-3 border border-gray-600 rounded-lg focus:outline-none focus:ring-2 focus:ring-gray-500 focus:border-transparent transition-all duration-200 bg-gray-700 text-white"
              >
                <option value="sunday" selected>Sunday</option>
                <option value="monday">Monday</option>
              </select>
            </div>
            <div class="flex justify-end pt-4">
              <button
                type="submit"
                class="bg-gradient-to-r from-gray-300 to-gray-400 hover:from-gray-400 hover:to-gray-500 text-gray-900 font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-105 shadow-lg"
              >
                💾 Save Preferences
              </button>
            </div>
          </form>
        </div>
      </div>
    </div>

    <!-- User Roles & Permissions -->
    <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
      <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
        <div class="flex items-center">
          <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
            <span class="text-white text-xl">👥</span>
          </div>
          <h3 class="text-xl font-semibold text-white">User Roles & Permissions</h3>
        </div>
        <p class="text-sm text-gray-400 mt-1">Configure user roles and their permissions</p>
      </div>
      <div class="p-6">
        <div class="space-y-6">
          <!-- Admin Role -->
          <div class="border border-gray-600 rounded-xl p-6 bg-gradient-to-r from-gray-700 to-gray-600">
            <div class="flex items-center justify-between mb-6">
              <div class="flex items-center">
                <div class="w-12 h-12 bg-gray-600 rounded-xl flex items-center justify-center mr-4">
                  <span class="text-white text-xl">👑</span>
                </div>
                <div>
                  <h4 class="text-xl font-semibold text-white">Admin</h4>
                  <p class="text-sm text-gray-300">Full system access and control</p>
                </div>
              </div>
              <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-semibold bg-gray-600 text-white">
                Default Role
              </span>
            </div>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" checked disabled class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">Manage Users</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" checked disabled class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">Manage Projects</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" checked disabled class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">View Reports</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" checked disabled class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">System Settings</span>
              </div>
            </div>
          </div>

          <!-- User Role -->
          <div class="border border-gray-600 rounded-xl p-6 bg-gradient-to-r from-gray-700 to-gray-600">
            <div class="flex items-center justify-between mb-6">
              <div class="flex items-center">
                <div class="w-12 h-12 bg-gray-600 rounded-xl flex items-center justify-center mr-4">
                  <span class="text-white text-xl">👤</span>
                </div>
                <div>
                  <h4 class="text-xl font-semibold text-white">User</h4>
                  <p class="text-sm text-gray-300">Standard user with limited access</p>
                </div>
              </div>
              <span class="inline-flex items-center px-3 py-1 rounded-full text-sm font-semibold bg-gray-600 text-white">
                Standard Role
              </span>
            </div>
            <div class="grid grid-cols-2 md:grid-cols-4 gap-4">
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">View Projects</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" checked class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">Track Time</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">View Reports</span>
              </div>
              <div class="flex items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <input type="checkbox" class="rounded border-gray-500 text-gray-300 focus:ring-gray-500 bg-gray-600" />
                <span class="ml-3 text-sm font-medium text-white">Manage Tasks</span>
              </div>
            </div>
          </div>

          <div class="flex justify-end pt-4">
            <button
              class="bg-gradient-to-r from-gray-300 to-gray-400 hover:from-gray-400 hover:to-gray-500 text-gray-900 font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-105 shadow-lg"
            >
              💾 Save Role Permissions
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- Project Templates -->
    <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
      <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
        <div class="flex items-center">
          <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
            <span class="text-white text-xl">📋</span>
          </div>
          <h3 class="text-xl font-semibold text-white">Project Templates</h3>
        </div>
        <p class="text-sm text-gray-400 mt-1">Create reusable project and task structures</p>
      </div>
      <div class="p-6">
        <div class="space-y-4">
          <div class="flex justify-between items-center p-4 bg-gradient-to-r from-gray-700 to-gray-600 rounded-xl border border-gray-600">
            <div class="flex items-center">
              <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-4">
                <span class="text-white text-lg">🌐</span>
              </div>
              <div>
                <h4 class="text-lg font-semibold text-white">Web Development Template</h4>
                <p class="text-sm text-gray-300">Standard tasks for web development projects</p>
              </div>
            </div>
            <div class="flex space-x-2">
              <button class="px-4 py-2 bg-gray-300 hover:bg-gray-400 text-gray-900 font-medium rounded-lg transition-colors">Edit</button>
              <button class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-medium rounded-lg transition-colors">Delete</button>
            </div>
          </div>
          <div class="flex justify-between items-center p-4 bg-gradient-to-r from-gray-700 to-gray-600 rounded-xl border border-gray-600">
            <div class="flex items-center">
              <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-4">
                <span class="text-white text-lg">🎨</span>
              </div>
              <div>
                <h4 class="text-lg font-semibold text-white">Design Project Template</h4>
                <p class="text-sm text-gray-300">Common tasks for design projects</p>
              </div>
            </div>
            <div class="flex space-x-2">
              <button class="px-4 py-2 bg-gray-300 hover:bg-gray-400 text-gray-900 font-medium rounded-lg transition-colors">Edit</button>
              <button class="px-4 py-2 bg-red-600 hover:bg-red-700 text-white font-medium rounded-lg transition-colors">Delete</button>
            </div>
          </div>
          <div class="flex justify-end pt-4">
            <button
              class="bg-gradient-to-r from-gray-300 to-gray-400 hover:from-gray-400 hover:to-gray-500 text-gray-900 font-semibold py-3 px-6 rounded-lg transition-all duration-200 transform hover:scale-105 shadow-lg"
            >
              ➕ Add Template
            </button>
          </div>
        </div>
      </div>
    </div>

    <!-- System Information -->
    <div class="bg-gray-800 rounded-xl shadow-sm border border-gray-700 overflow-hidden">
      <div class="px-6 py-4 bg-gradient-to-r from-gray-800 to-gray-700 border-b border-gray-700">
        <div class="flex items-center">
          <div class="w-10 h-10 bg-gray-600 rounded-lg flex items-center justify-center mr-3">
            <span class="text-gray-300 text-xl">ℹ️</span>
          </div>
          <h3 class="text-xl font-semibold text-white">System Information</h3>
        </div>
      </div>
      <div class="p-6">
        <div class="grid grid-cols-1 md:grid-cols-2 gap-8">
          <div class="bg-gradient-to-r from-gray-700 to-gray-600 p-6 rounded-xl border border-gray-600">
            <h4 class="text-lg font-semibold text-white mb-4 flex items-center">
              <span class="mr-2">🚀</span>
              Application
            </h4>
            <div class="space-y-3 text-sm">
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Version:</span>
                <span class="text-gray-300 font-semibold">1.0.0</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Environment:</span>
                <span class="text-gray-300 font-semibold">Production</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Database:</span>
                <span class="text-gray-300 font-semibold">PostgreSQL (Neon)</span>
              </div>
            </div>
          </div>
          <div class="bg-gradient-to-r from-gray-700 to-gray-600 p-6 rounded-xl border border-gray-600">
            <h4 class="text-lg font-semibold text-white mb-4 flex items-center">
              <span class="mr-2">🖥️</span>
              Server
            </h4>
            <div class="space-y-3 text-sm">
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Platform:</span>
                <span class="text-gray-300 font-semibold">Astro + React</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Node.js:</span>
                <span class="text-gray-300 font-semibold">v18+</span>
              </div>
              <div class="flex justify-between items-center p-3 bg-gray-600 rounded-lg border border-gray-500">
                <span class="font-medium text-gray-300">Last Updated:</span>
                <span class="text-gray-300 font-semibold">{new Date().toLocaleDateString()}</span>
              </div>
            </div>
          </div>
        </div>
      </div>
    </div>
  </div>

  <script>
    // Settings JavaScript
    const orgForm = document.getElementById('orgForm');
    const systemForm = document.getElementById('systemForm');

    if (orgForm) {
      orgForm.addEventListener('submit', async (e) => {
        e.preventDefault();
        const orgName = (document.getElementById('orgName') as HTMLInputElement)?.value;
        const orgEmail = (document.getElementById('orgEmail') as HTMLInputElement)?.value;
        const orgWebsite = (document.getElementById('orgWebsite') as HTMLInputElement)?.value;
        const orgLogo = (document.getElementById('orgLogo') as HTMLInputElement)?.value;
        
        try {
          const response = await fetch('/api/admin/settings/organization', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ 
              name: orgName,
              email: orgEmail,
              website: orgWebsite,
              logo: orgLogo
            }),
          });
          
          if (response.ok) {
            alert('Organization settings saved successfully');
          } else {
            alert('Error saving organization settings');
          }
        } catch (error) {
          console.error('Error:', error);
          alert('Error saving organization settings');
        }
      });
    }

    if (systemForm) {
      systemForm.addEventListener('submit', async (e) => {
        e.preventDefault();
        const timezone = (document.getElementById('timezone') as HTMLSelectElement)?.value;
        const dateFormat = (document.getElementById('dateFormat') as HTMLSelectElement)?.value;
        const timeFormat = (document.getElementById('timeFormat') as HTMLSelectElement)?.value;
        const weekStart = (document.getElementById('weekStart') as HTMLSelectElement)?.value;
        
        try {
          const response = await fetch('/api/admin/settings/system', {
            method: 'POST',
            headers: { 'Content-Type': 'application/json' },
            body: JSON.stringify({ 
              timezone,
              dateFormat,
              timeFormat,
              weekStart
            }),
          });
          
          if (response.ok) {
            alert('System preferences saved successfully');
          } else {
            alert('Error saving system preferences');
          }
        } catch (error) {
          console.error('Error:', error);
          alert('Error saving system preferences');
        }
      });
    }
  </script>
</AdminLayout> 